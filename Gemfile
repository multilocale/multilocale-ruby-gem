# frozen_string_literal: true

source "https://rubygems.org"

# The gem itself, plus its (empty) runtime dependency list.
gemspec

group :development, :test do
  gem "rake", "~> 13.3"
  gem "rspec", "~> 3.13"
end
